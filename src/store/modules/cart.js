const state = {
  cart: {}
}

const getters = {
  cart: (state) => state.cart
}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}